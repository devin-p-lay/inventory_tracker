<h1>Create a New Item</h1>

<%= form_with url: "/warehouses/#{@warehouse.id}/items", method: :post, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit 'Create New Item' %>
<% end %>